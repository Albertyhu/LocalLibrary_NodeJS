<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>My Website</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link rel="icon" href="./favicon.ico" type="image/x-icon">
  </head>
  <body>
    <div class = "container-fluid">
        <div class = "row">
            <div class = "col-sm-2 layout">
                <div>
                <%- include("partials/sidebar") %>
                </div>
                <div>
                    <h1><%= title %></h1> 
                    <% if(bookinstance_list){%>
                        <ul>
                        <% bookinstance_list.forEach(val => {%>
                            <li>
                                <a href="<%= val.url %>" > <%= val.book.title %> : <%= val.imprint %> - </a>
                                    <% if(val.status == "Available") { %>
                                        <span class = "text-success"><%=val.status%></span>
                                        <% } else if(val.status == "Maintenance"){ %>
                                        <span class = "text-danger"><%=val.status%></span>
                                        <% } else { %>
                                        <span class = "text-warning"><%=val.status%></span>
                                        <% } %>
                                    <% if(val.status != "Available") { %>
                                        <span class = "">Due: <%=val.due_back_formatted%></span>
                                         <% } %>
                            </li>
                        <% }) %>
                        </ul>
                    <% } else { %>
                        <p>There are no copies of the book.</p>
                    <% }%>
                </div>
            </div>
        </div>
    </div>
    </main>
	<script src="index.js"></script>
  </body>
</html>
